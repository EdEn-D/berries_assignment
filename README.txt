# Instuctions
For this POC the demo notebook is the best way to demonstrate the workflow. 

First, insert the API keys into the example env file and rename to .env
Second, install the packages in requirments.txt
Finally run the chains through the notebook

